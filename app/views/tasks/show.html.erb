<div class = "container">
  <h1>Hello from show!</h1>

<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title"><%= @task.title %></h5>
    <h6 class="card-subtitle mb-2 text-muted">Task done: <%= @task.completed %></h6>
    <p class="card-text">Task details: <%= @task.details %></p>
  </div>
</div>
</div>
